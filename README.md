# Toxic Comment Classification

Этот проект представляет собой решение задачи классификации токсичных комментариев (Multi-label text classification). Модель определяет вероятность принадлежности текста к одному или нескольким из 6 классов: `toxic`, `severe_toxic`, `obscene`, `threat`, `insult`, `identity_hate`.

## 📊 Описание данных

В проекте используется набор данных, содержащий комментарии из Википедии, размеченные людьми по уровню токсичности.
Целевые метки:
*   Toxic (Токсичный)
*   Severe Toxic (Сильно токсичный)
*   Obscene (Непристойный)
*   Threat (Угроза)
*   Insult (Оскорбление)
*   Identity Hate (Ненависть по признаку идентичности)

### Баланс классов

В датасете наблюдается сильный дисбаланс: большая доля комментариев помечена как нетоксичные, тогда как положительные метки (особенно `threat` и `severe_toxic`) встречаются редко. Этот дисбаланс влияет на метрики — особенно на Precision и F1 для редких классов — и требует специальных подходов при обучении и оценке (взвешивание классов, ресемплинг, подбор порогов, оценка на уровне каждого класса).

## 🛠 Используемые технологии

*   **Python 3.x**
*   **Pandas / NumPy**: Обработка данных
*   **Scikit-learn**:
    *   `TfidfVectorizer`: Преобразование текста в векторы (TF-IDF).
    *   `LogisticRegression`: Базовая модель (Solver: liblinear).
    *   `OneVsRestClassifier`: Стратегия обучения для многометочной классификации (обучается отдельный классификатор для каждого класса).
    *   `Pipeline`: Пайплайн для автоматизации предобработки и обучения.
*   **Matplotlib**: Визуализация распределения данных и метрик.
*   **SHAP**: Интерпретация предсказаний модели (Feature Importance).

## 🚀 Основные этапы проекта

1.  **EDA (Exploratory Data Analysis)**: Анализ баланса классов.
2.  **Препроцессинг**: Очистка текста (удаление спецсимволов, приведение к нижнему регистру).
3.  **Обучение (Baseline)**: TF-IDF + Логистическая регрессия.
4.  **Валидация**: Расчет метрик.
5.  **Оптимизация порогов (Threshold Tuning)**: Подбор оптимального порога классификации для каждого класса на основе максимизации F1-score.
6.  **FastText + Логистическая регрессия**: Использование эмбеддингов FastText для векторизации текста.
7. **Валидация и оптимизация FastText модели**: Расчет метрик с оптимизированными порогами.
8.  **Интерпретация**:
    *   Анализ весов модели (Top features).
    *   Объяснение предсказаний с помощью библиотеки SHAP.
9. **BERT + Логистическая регрессия**: Использование эмбеддингов DistilBERT для векторизации текста

---



## 📁 Структура проекта

```
toxic_text/
├── data/
│   ├── train.csv          # Обучающий датасет
│   └── test.csv           # Тестовый датасет
├── src/
│   ├── __init__.py
│   ├── preprocess_data.py # Функции очистки текста (clean_text, preprocess_data)
│   ├── utilits.py         # Утилиты (metrics_model, explain_text, applay_trasholds)
│   └── find_optimal_threshold.py  # Поиск оптимальных порогов
├── bert_tox_class.ipynb             # Notebook с BERT моделью
├── main.ipynb             # Notebook с основными результатами
└── README.md
```

## 📊 Результаты моделей

### TF-IDF + Logistic Regression (Baseline)
- **ROC-AUC**: ~0.972
- F1 Score (macro): 0.5918
- F1 Score (micro): 0.7144
- Accuracy: 0.9094
- Precision: 0.3888
- Recall: 0.6129
### FastText + Logistic Regression
- **ROC-AUC**: ~0.82
- F1 Score (macro): 0.2561
- F1 Score (micro): 0.3425
- Accuracy: 0.8285
- Precision: 0.1062
- Recall: 0.3038
### BERT + Logistic Regression
- **ROC-AUC**: ~0.977
- F1 Score (macro): 0.6039
- F1 Score (micro): 0.7130
- Accuracy: 0.9082
- Precision: 0.3973
- Recall: 0.6274
---

# **Реализовано:**
- Baseline модель с ROC-AUC 0.96+
- Оптимизация порогов классификации для каждого класса
- Анализ важных признаков (топовые токсичные/нейтральные слова)
- Объяснение предсказаний для конкретных текстов
- Сравнение моделей (TF-IDF, FastText, BERT)
- Использование SHAP для интерпретации

## 🔎 Объяснение результатов и интерпретация

- **Высокий ROC-AUC, но низкая точность (Precision)**: ROC-AUC показывает общую способность модели ранжировать положительные примеры выше отрицательных, что может оставаться высоким при дисбалансе классов. Однако в условиях редких положительных меток модель может давать много ложноположительных срабатываний, что снижает Precision.
- **Разница между macro и micro F1**: Micro-F1 больше учитывает частые классы (влияет доминирующее количество нетоксичных примеров), тогда как Macro-F1 усредняет по классам и сильнее страдает из-за плохой работы на редких классах. Низкий Macro-F1 указывает на то, что производительность по некоторым классам значительно хуже.
- **TF-IDF vs FastText**: В этой реализации TF-IDF + Logistic Regression показывает лучшее качество по большинству метрик по сравнению с FastText; это может быть связано с тем, что для коротких комментариев сильно выраженные токены (ключевые слова) оказываются информативнее, чем усреднённые эмбеддинги FastText без более сложной архитектуры.
- **Влияние дисбаланса**: Редкие классы (например, `threat`, `severe_toxic`) имеют меньше примеров для обучения — это приводит к высокой вариативности оценок и плохой обобщающей способности. Простые модели без компенсации дисбаланса склонны недооценивать такие классы.
- **Роль оптимизации порогов**: Подбор порога классификации отдельно для каждого класса (threshold tuning) улучшает F1 для редких классов по сравнению с порогом 0.5, потому что позволяет найти компромисс между Precision и Recall, ориентируясь на целевую метрику (например, F1).

Возможности для улучшения результатов:
- Оценивать метрики по классам и смотреть confusion matrix для каждого лейбла.
- Использовать взвешивание классов (`class_weight`) или ресемплинг (oversampling положительных примеров, SMOTE для текстовых признаков с осторожностью).
- Проанализировать какие токены/признаки приводят к ложным срабатываниям.




## 📚 Используемые ресурсы

- [Kaggle Toxic Comment Classification](https://www.kaggle.com/c/jigsaw-toxic-comment-classification-challenge)
- [Scikit-learn Multi-label Classification](https://scikit-learn.org/stable/modules/multiclass.html)
- [Gensim FastText](https://radimrehurek.com/gensim/models/fasttext.html)
- [SHAP Documentation](https://shap.readthedocs.io/)

---

## 📊 Основные метрики для оценки

- **ROC-AUC**: Общая производительность модели
- **F1-score (Macro)**: Среднее по всем классам
- **Precision**: Доля правильных предсказаний среди положительных
- **Recall**: Доля найденных позитивных примеров


---

## 🤝 Вклад

Это личный проект для обучения. Рекомендации и улучшения приветствуются!

